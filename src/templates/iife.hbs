// This file is auto-generated from the modulize script. Please do not modify this file.

/* eslint-disable */
const icon = (name, data) => ({
  name,
  data,
  options: {
    'version': '1.1',
    'width': data.width,
    'height': data.height,
    'viewBox': `0 0 ${data.width} ${data.height}`,
    'class': `octicon octicon-${name}`,
    'aria-hidden': 'true'
  },

{{> iconMethods }}
})

export default {
{{#each icons}}  {{ camelName }}: icon('{{ name }}', {{{ jsonObject data }}}){{#unless @last}},{{/unless}}
{{/each}}
}

